accessing
verbose
	"indicates that the process will be executed in 'verbose' mode: without redirect stdout/stderr to 
	 internal scripts which means those will still be going to the console"
	verbose := true